# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependency directories
node_modules/

# Build files and distributions
dist/
dist-ssr/

# Local environment settings
*.local
.env          # Ensure backend environment variables are excluded
.env.local    # In case there are environment overrides
.env.*.local

# Uploads (in backend)
uploads/

# Editor directories and files
.vscode/
!.vscode/extensions.json  # If you want to share recommended extensions
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Lock files (Optional - depending on your version control preference)
package-lock.json
pnpm-lock.yaml
yarn.lock

# Temporary files and caches
.cache/
tmp/
